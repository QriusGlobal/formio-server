# GitHub Packages Configuration for @qrius scope
@qrius:registry=https://npm.pkg.github.com

# Authentication token (set via environment variable or local ~/.npmrc)
# For CI/CD: Uses GITHUB_TOKEN automatically
# For local dev: Set GITHUB_TOKEN or use personal .npmrc
# Conditional expansion - only uses token if GITHUB_TOKEN is set
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
