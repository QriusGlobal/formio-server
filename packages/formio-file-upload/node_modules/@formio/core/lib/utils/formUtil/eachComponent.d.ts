import { Component, EachComponentCallback, ComponentPaths } from '../../types';
/**
 * Iterate through each component within a form.
 *
 * @param {Object} components
 *   The components to iterate.
 * @param {Function} fn
 *   The iteration function to invoke for each component.
 * @param {Boolean} includeAll
 *   Whether or not to include layout components.
 * @param {Object} parent
 *   The parent object.
 */
export declare function eachComponent(components: Component[], fn: EachComponentCallback, includeAll?: boolean, parentPaths?: string | ComponentPaths, parent?: Component): void;
